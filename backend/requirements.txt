fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.9
pydantic[email]>=2.0.0
pydantic-settings>=2.0.0
httpx>=0.27.0
# amieclient is installed via Dockerfile with --no-deps to avoid its stale
# python-dateutil<2.7 constraint, which is broken on Python 3.11+.
# Its other dependency (requests) is listed here explicitly as a result.
requests>=2.20.0
python-dateutil>=2.8.2
python-multipart>=0.0.9
itsdangerous>=2.2.0
