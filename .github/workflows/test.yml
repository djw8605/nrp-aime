name: Tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  backend-tests:
    name: Backend Tests
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install --no-cache-dir "amieclient>=0.4.0" --no-deps
          pip install --no-cache-dir -r requirements.txt

      - name: Run tests
        run: python -m pytest tests/ -v --tb=short
