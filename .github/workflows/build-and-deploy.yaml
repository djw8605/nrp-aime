name: Build And Deploy

on:
  push:
    branches:
      - main

permissions:
  contents: write
  packages: write

concurrency:
  group: build-and-deploy-main
  cancel-in-progress: true

jobs:
  build-and-update:
    if: github.actor != 'github-actions[bot]' && !contains(github.event.head_commit.message, '[skip deploy]')
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.ref_name }}

      - name: Compute image variables
        id: vars
        shell: bash
        run: |
          owner="${GITHUB_REPOSITORY_OWNER,,}"
          repo="${GITHUB_REPOSITORY#*/}"
          repo="${repo,,}"
          echo "image_tag=${GITHUB_SHA}" >> "$GITHUB_OUTPUT"
          echo "backend_image=ghcr.io/${owner}/${repo}-backend" >> "$GITHUB_OUTPUT"
          echo "frontend_image=ghcr.io/${owner}/${repo}-frontend" >> "$GITHUB_OUTPUT"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: ${{ steps.vars.outputs.backend_image }}:${{ steps.vars.outputs.image_tag }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push frontend image
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: ${{ steps.vars.outputs.frontend_image }}:${{ steps.vars.outputs.image_tag }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Set up Kustomize
        uses: imranismail/setup-kustomize@v2

      - name: Update deployment image tags
        shell: bash
        working-directory: deployment
        run: |
          # Keep the deployment pinned to the exact images built from this commit.
          kustomize edit set image \
            ghcr.io/your-org/nrp-aime-backend=${{ steps.vars.outputs.backend_image }}:${{ steps.vars.outputs.image_tag }} \
            ghcr.io/your-org/nrp-aime-frontend=${{ steps.vars.outputs.frontend_image }}:${{ steps.vars.outputs.image_tag }}

      - name: Commit updated Kustomize file
        shell: bash
        run: |
          if git diff --quiet -- deployment/kustomization.yaml; then
            echo "No Kustomize changes to commit."
            exit 0
          fi

          # Commit only the deployment Kustomize file touched above.
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add deployment/kustomization.yaml
          git commit -m "chore: update deployment image tags for ${GITHUB_SHA} [skip deploy]"
          git push origin HEAD:${GITHUB_REF_NAME}
