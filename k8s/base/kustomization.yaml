apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nrp-aime

resources:
  - namespace.yaml
  - postgres-pvc.yaml
  - postgres-deployment.yaml
  - postgres-service.yaml
  - backend-deployment.yaml
  - backend-service.yaml
  - aime-worker-deployment.yaml
  - usage-worker-deployment.yaml
  - frontend-deployment.yaml
  - frontend-service.yaml
  - ingress.yaml

configMapGenerator:
  - name: nrp-aime-config
    envs:
      - config/app.env

secretGenerator:
  - name: nrp-aime-secrets
    envs:
      - config/secret.env

generatorOptions:
  disableNameSuffixHash: true

images:
  - name: ghcr.io/your-org/nrp-aime-backend
    newTag: latest
  - name: ghcr.io/your-org/nrp-aime-frontend
    newTag: latest
