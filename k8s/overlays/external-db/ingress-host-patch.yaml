apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nrp-aime
spec:
  rules:
    - host: nrp-aime.example.org
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 8000
          - path: /healthz
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 8000
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
  tls:
    - hosts:
        - nrp-aime.example.org
