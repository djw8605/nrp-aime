apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  template:
    spec:
      initContainers:
        - name: wait-for-db
          env:
            - name: POSTGRES_USER
              $patch: delete
            - name: POSTGRES_DB
              $patch: delete
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: nrp-aime-secrets
                  key: DATABASE_URL
          command:
            - sh
            - -c
            - until pg_isready -d "$DATABASE_URL"; do sleep 2; done
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: aime-worker
spec:
  template:
    spec:
      initContainers:
        - name: wait-for-db
          env:
            - name: POSTGRES_USER
              $patch: delete
            - name: POSTGRES_DB
              $patch: delete
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: nrp-aime-secrets
                  key: DATABASE_URL
          command:
            - sh
            - -c
            - until pg_isready -d "$DATABASE_URL"; do sleep 2; done
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: usage-worker
spec:
  template:
    spec:
      initContainers:
        - name: wait-for-db
          env:
            - name: POSTGRES_USER
              $patch: delete
            - name: POSTGRES_DB
              $patch: delete
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: nrp-aime-secrets
                  key: DATABASE_URL
          command:
            - sh
            - -c
            - until pg_isready -d "$DATABASE_URL"; do sleep 2; done
